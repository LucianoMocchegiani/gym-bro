# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
*.code-workspace

# Env / secrets
.env
.env.*
!.env.example
*.pem
credentials.json

# Dependencies / build
node_modules/
dist/
build/
coverage/
.next/
.turbo/
.dart_tool/
*.iml

# Logs
*.log
npm-debug.log*

# Local
tmp/
.temp/
local/

# Clon local: package Flutter identity_core_dart (wallet OID4)
# Issuer/verifier: Kuatia en dominio (no se clonan acá)
identity_core_dart/

# `git-hooks/` del repo se versiona (plantillas). `.git/hooks/` es local y no entra en git.
