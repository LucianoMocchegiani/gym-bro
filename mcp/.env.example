# Copiar a mcp/.env (no se commitea).
# Compose carga este archivo en el servicio `mcp`.

NODE_ENV=development
PORT=3011

# Nest GymBro (red Compose). En host: http://localhost:3001
GYMBRO_API_URL=http://api:3001
